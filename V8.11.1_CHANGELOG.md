# V8.11.1 변경사항

1. 실제 V8.11 `monitor.py`의 `process()` 흐름에 추가 탐색 로직을 연결했습니다.
2. 기존 `discover_board()`가 실패한 경우에만 추가 탐색을 수행합니다.
3. 추가 탐색은 과도한 크롤링을 피하도록 메뉴 페이지 8개, 후보 25개로 제한합니다.
4. `board_discovery_log.json`에 기관별 진단 결과를 기록합니다.
5. 새로 발견된 게시판 URL을 `state.json`에 저장합니다.
6. Telegram 대기열, 키워드 3개, 제목 `공모전` 제외 정책은 유지합니다.
